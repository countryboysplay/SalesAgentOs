/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/**
 * Ambient types for the Vite build. `vite-plugin-pwa/client` is what declares
 * the `virtual:pwa-register` module used in main.tsx.
 *
 * Owner: Design System (app shell). Adding a reference here is the supported
 * way to extend types without touching the shared tsconfig `types` array.
 */
